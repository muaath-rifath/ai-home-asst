import { IHttpClient } from './IHttpClient';
declare type Fetch = (url: string, init?: any) => Promise<any>;
export declare class HttpClientFactory {
    static Create(fetch: Fetch): IHttpClient;
}
export {};
