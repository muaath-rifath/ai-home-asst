import { CloudOptions } from '../CloudOptions';
import { IMultiPropertiesCloudClient } from '../client/ICloudClient';
import { ICloudClientBuilder, MqttConnection } from './ICloudClientBuilder';
export declare type BrowserOptions = {
    token: string;
};
export declare class TokenClientBuilder implements ICloudClientBuilder {
    private mqttConnect;
    constructor(mqttConnect: MqttConnection);
    canBuild(options: BrowserOptions & CloudOptions): boolean;
    build(options: BrowserOptions & CloudOptions): Promise<IMultiPropertiesCloudClient>;
    private connection;
}
